# Etch-a-Sketch
A browser version of something between a sketchpad and an Etch-A-Sketch.

Grid is filled with random colors that increase by 10% opacity with each interaction. User defines grid size. (Max 100 x 100)
